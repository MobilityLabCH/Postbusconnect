# PostBus Connect — Ready to deploy (Carto tile fix)
- `npm install && npm run dev` → http://localhost:4000
- Static site in `public/`
- API (optional) in `server/`
- Tiles via CartoCDN with OSM fallback.
Generated: 2025-08-15
